"""Shared constants for Performance Dashboard."""

# Build-safe defaults when client/account targets are not configured.
DEFAULT_TARGET_ROAS = 3.0
DEFAULT_TARGET_TACOS = 15.0  # 15 % — percentage scale (15.0 means 15%)

